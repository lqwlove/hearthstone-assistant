## 1. Project scaffolding

- [x] 1.1 初始化 FastAPI 后端目录、依赖、环境变量模板（JWT、数据库、暴雪、LLM）与本地启动说明
- [x] 1.2 初始化 React 前端（PC 宽屏）、路由骨架、API 客户端与全局导航壳
- [x] 1.3 配置数据库连接与迁移工具；建立基础 User / Card / Deck / DeckCard / ChatThread / ChatMessage 表结构

## 2. User authentication

- [x] 2.1 实现注册与登录 API（密码哈希、JWT 签发）及未授权错误行为
- [x] 2.2 实现鉴权依赖/中间件，保护私有路由
- [x] 2.3 前端登录/注册页与令牌存储；请求自动附带 Bearer Token

## 3. Card catalog and Blizzard sync

- [x] 3.1 实现暴雪 OAuth/客户端凭证与 Hearthstone 卡牌拉取（`zh_CN`），映射写入本地 Card 表
- [x] 3.2 实现受保护的手动同步接口及失败时保留旧数据的错误处理
- [x] 3.3 实现卡牌列表检索/筛选 API（费用、职业、稀有度、关键词、模式相关过滤）与详情 API
- [x] 3.4 前端牌库页（浏览、筛选、搜索）与卡牌详情页/视图

## 4. Deck building and validation

- [x] 4.1 实现卡组创建、列表、读取、更新草稿（含卡牌张数）API，并强制用户隔离
- [x] 4.2 实现构筑校验引擎（30 张、职业+中立、同名/传说上限、标准/狂野合法性）与最终保存 API
- [x] 4.3 前端我的卡组列表与创建流程（名称、职业、模式）
- [x] 4.4 实现独立三栏组牌页：左牌池、中操作区（增减、张数/校验状态、草稿保存与最终保存）、顶栏无全局左侧菜单

## 5. Deck assistant

- [x] 5.1 实现每卡组单对话线程的消息持久化 API（历史加载、权限校验）
- [x] 5.2 实现 LLM 适配层（OpenAI/Claude 兼容配置、统一服务端 Key）与助手回复生成
- [x] 5.3 解析可选结构化 `deck_patch`；合法则应用到草稿，非法则安全忽略并提示
- [x] 5.4 组牌页右侧对话区：发送消息、展示历史、有 patch 时刷新中间卡组

## 6. Hardening and developer experience

- [x] 6.1 为校验引擎、鉴权与关键 API 补充自动化测试
- [x] 6.2 补充 README：启动步骤、所需环境变量、首次同步卡牌流程
- [x] 6.3 端到端走通：注册登录 → 同步/浏览牌库 → 组套草稿 → 助手对话 → 合法最终保存
